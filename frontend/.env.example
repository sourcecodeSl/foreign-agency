# Copy to .env

# true  -> screens run on the built-in mock adapter, no backend needed
# false -> calls the live Laravel API (proxied to http://localhost:8000)
VITE_USE_MOCK=true

VITE_API_URL=/api/v1
