# Copy to .env

# true  -> screens run on the built-in mock adapter, no backend needed
# false -> calls the live Express API (proxied to http://localhost:5000)
VITE_USE_MOCK=true

VITE_API_URL=/api/v1
